/*
 * These classes each have a getFlags() method that overrides
 * DBusProxy.getFlags() with a different enum return type. This is not allowed
 * in Java. A possible solution could be to specifiy the flags type with a
 * generic parameter, but the Java-GI code generator doesn't support that,
 * so we just rename each getFlags() methods to getNNNFlags().
 */
Item.get_flags name=get_item_flags
Collection.get_flags name=get_collection_flags
Service.get_flags name=get_service_flags
