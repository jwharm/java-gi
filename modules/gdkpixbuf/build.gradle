plugins {
    id 'java-gi.library-conventions'
}

dependencies {
    api project(':gmodule')
    api project(':gio')
}

generateSources.configure {
    namespace = 'GdkPixbuf'
    urlPrefix = 'https://docs.gtk.org/gdk-pixbuf/'
}
