plugins {
    id 'java-gi.library-conventions'
}

dependencies {
    api project(':gst')
    api project(':gstaudio')
    api project(':gstbase')
    api project(':gstvideo')
}

tasks.named('generateSources') {
    girFile = 'GstPbutils-1.0.gir'
}
