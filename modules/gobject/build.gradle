import io.github.jwharm.javagi.patches.GObjectPatch;

plugins {
    id 'java-gi.library-conventions'
}

dependencies {
    api project(':glib')
}

tasks.named('generateSources') {
    girFile = 'GObject-2.0.gir'
    urlPrefix = 'https://docs.gtk.org/glib/'
    patch = new GObjectPatch()
}
