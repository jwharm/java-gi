import io.github.jwharm.javagi.patches.GioPatch;

plugins {
    id 'java-gi.library-conventions'
}

dependencies {
    api project(':gobject')
}

tasks.named('generateSources') {
    girFile = 'Gio-2.0.gir'
    urlPrefix = 'https://docs.gtk.org/glib/'
    patch = new GioPatch()
}
